import { mockEventDetails } from "../../../../../src/utils/mock-data"

async function getGuestListData(id) {

}

export default function Page({ params } : { params: { id }}) {
    
    return (
        <></>
    )
}